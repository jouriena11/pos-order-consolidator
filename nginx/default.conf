server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/src/app/client/build;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
    
    location /api/ {
        proxy_pass http://pos-nodejs:4003;
    }
}
